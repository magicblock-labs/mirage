interface:
  display_name: "Mirage Private Transfer"
  short_description: "Receive funds and send Mirage private transfers"
  default_prompt: "Use $mirage-private-transfer to receive funds with Mirage or send a private Mirage transfer safely."

policy:
  allow_implicit_invocation: true
